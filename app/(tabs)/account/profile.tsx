import { Redirect } from "expo-router";
import { Text, View } from "react-native";

export default function ProfileScreen() {

    // Affichage du profil
    // Bouton edit qui emmène vers profile-edit.tsx
    // Supprimer le compte ? 
    // Bouton deconnexion qui deconnecte l'utilisateur et le redirige vers login.tsx 
        

    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Affichage de profil</Text>
        </View>
    )
};



